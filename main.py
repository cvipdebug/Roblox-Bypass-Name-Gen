import random, string, requests, threading
from colorama import Fore
from os import system

system("title Cvip Roblox Bypass Name Gen && MODE 65,18")

proxies = open("proxy.txt").read().splitlines()


class title:
    woeking = 0
    notworking = 0


def getProxy():
    return random.choice(proxies)


amount = int(input("How many threds? : "))


def pussylicker():
    firstPart = "pus"
    # the best is 2 up
    random_shit = ("").join(random.choice(string.ascii_letters) for k in range(2))
    EndPart = "syeater"
    name = f"{firstPart}{random_shit}{EndPart}"
    checker = requests.get(
        f"https://auth.roblox.com/v2/usernames/validate?request.username={name}&request.birthday=2000-01-01&request.context=Signup",
        proxies={"http": 'http://' + getProxy()}).json()
    code1 = checker['message']

    if code1 == "Username is valid":
        title.woeking += 1
        print("[" + Fore.GREEN + "INFO" + Fore.WHITE + f"] Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))
        with open("Names.txt", "a") as f:
            f.write(f"{name}\n")
            f.close()
    else:
        title.notworking += 1
        print("[" + Fore.RED + "INFO" + Fore.WHITE + f"] Not Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))


def pussy():
    firstPart = "pus"
    # the best is 2 up
    random_shit = ("").join(random.choice(string.ascii_letters) for k in range(2))
    EndPart = "sy"
    name = f"{firstPart}{random_shit}{EndPart}"
    checker = requests.get(
        f"https://auth.roblox.com/v2/usernames/validate?request.username={name}&request.birthday=2000-01-01&request.context=Signup",
        proxies={"http": 'http://' + getProxy()}).json()
    code1 = checker['message']

    if code1 == "Username is valid":
        title.woeking += 1
        print("[" + Fore.GREEN + "INFO" + Fore.WHITE + f"] Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))
        with open("Names.txt", "a") as f:
            f.write(f"{name}\n")
            f.close()
    else:
        title.notworking += 1
        print("[" + Fore.RED + "INFO" + Fore.WHITE + f"] {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))


def assbeater420():
    firstPart = "as"
    # the best is 2 up
    random_shit = ("").join(random.choice(string.ascii_letters) for k in range(1))
    EndPart = "sBeater42I0"
    name = f"{firstPart}{random_shit}{EndPart}"
    checker = requests.get(
        f"https://auth.roblox.com/v2/usernames/validate?request.username={name}&request.birthday=2000-01-01&request.context=Signup",
        proxies={"http": 'http://' + getProxy()}).json()
    code1 = checker['message']

    if code1 == "Username is valid":
        title.woeking += 1
        print("[" + Fore.GREEN + "INFO" + Fore.WHITE + f"] Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))
        with open("Names.txt", "a") as f:
            f.write(f"{name}\n")
            f.close()
    else:
        title.notworking += 1
        print("[" + Fore.RED + "INFO" + Fore.WHITE + f"] Not Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))


def nigger():
    firstPart = "ni"
    # the best is 2 up
    random_shit = ("").join(random.choice(string.ascii_letters) for k in range(2))
    EndPart = "gger"
    name = f"{firstPart}{random_shit}{EndPart}"
    checker = requests.get(
        f"https://auth.roblox.com/v2/usernames/validate?request.username={name}&request.birthday=2000-01-01&request.context=Signup",
        proxies={"http": 'http://' + getProxy()}).json()
    code1 = checker['message']

    if code1 == "Username is valid":
        title.woeking += 1
        print("[" + Fore.GREEN + "INFO" + Fore.WHITE + f"] Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))
        with open("Names.txt", "a") as f:
            f.write(f"{name}\n")
            f.close()
    else:
        title.notworking += 1
        print("[" + Fore.RED + "INFO" + Fore.WHITE + f"] Not Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))


def Nigga():
    firstPart = "nig"
    # the best is 2 up
    random_shit = ("").join(random.choice(string.ascii_letters) for k in range(2))
    EndPart = "ga"
    name = f"{firstPart}{random_shit}{EndPart}"
    checker = requests.get(
        f"https://auth.roblox.com/v2/usernames/validate?request.username={name}&request.birthday=2000-01-01&request.context=Signup",
        proxies={"http": 'http://' + getProxy()}).json()
    code1 = checker['message']

    if code1 == "Username is valid":
        title.woeking += 1
        print("[" + Fore.GREEN + "INFO" + Fore.WHITE + f"] Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))
        with open("Names.txt", "a") as f:
            f.write(f"{name}\n")
            f.close()
    else:
        title.notworking += 1
        print("[" + Fore.RED + "INFO" + Fore.WHITE + f"] Not Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))


def Fuck():
    firstPart = "fu"
    # the best is 2 up
    random_shit = ("").join(random.choice(string.ascii_letters) for k in range(2))
    EndPart = "qk"
    name = f"{firstPart}{random_shit}{EndPart}"
    checker = requests.get(
        f"https://auth.roblox.com/v2/usernames/validate?request.username={name}&request.birthday=2000-01-01&request.context=Signup",
        proxies={"http": 'http://' + getProxy()}).json()
    code1 = checker['message']

    if code1 == "Username is valid":
        title.woeking += 1
        print("[" + Fore.GREEN + "INFO" + Fore.WHITE + f"] Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))
        with open("Names.txt", "a") as f:
            f.write(f"{name}\n")
            f.close()
    else:
        title.notworking += 1
        print("[" + Fore.RED + "INFO" + Fore.WHITE + f"] Not Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))


def Bitch():
    firstPart = "bit"
    # the best is 2 up
    random_shit = ("").join(random.choice(string.ascii_letters) for k in range(2))
    EndPart = "ch"
    name = f"{firstPart}{random_shit}{EndPart}"
    checker = requests.get(
        f"https://auth.roblox.com/v2/usernames/validate?request.username={name}&request.birthday=2000-01-01&request.context=Signup",
        proxies={"http": 'http://' + getProxy()}).json()
    code1 = checker['message']

    if code1 == "Username is valid":
        title.woeking += 1
        print("[" + Fore.GREEN + "INFO" + Fore.WHITE + f"] Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))
        with open("Names.txt", "a") as f:
            f.write(f"{name}\n")
            f.close()
    else:
        title.notworking += 1
        print("[" + Fore.RED + "INFO" + Fore.WHITE + f"] Not Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))


def Shit():
    firstPart = "sh"
    # the best is 2 up
    random_shit = ("").join(random.choice(string.ascii_letters) for k in range(2))
    EndPart = "it"
    name = f"{firstPart}{random_shit}{EndPart}"
    checker = requests.get(
        f"https://auth.roblox.com/v2/usernames/validate?request.username={name}&request.birthday=2000-01-01&request.context=Signup",
        proxies={"http": 'http://' + getProxy()}).json()
    code1 = checker['message']

    if code1 == "Username is valid":
        title.woeking += 1
        print("[" + Fore.GREEN + "INFO" + Fore.WHITE + f"] Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))
        with open("Names.txt", "a") as f:
            f.write(f"{name}\n")
            f.close()
    else:
        title.notworking += 1
        print("[" + Fore.RED + "INFO" + Fore.WHITE + f"] Not Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))


def Ass():
    firstPart = "as"
    # the best is 3 up
    random_shit = ("").join(random.choice(string.ascii_letters) for k in range(3))
    EndPart = "s"
    name = f"{firstPart}{random_shit}{EndPart}"
    checker = requests.get(
        f"https://auth.roblox.com/v2/usernames/validate?request.username={name}&request.birthday=2000-01-01&request.context=Signup",
        proxies={"http": 'http://' + getProxy()}).json()
    code1 = checker['message']

    if code1 == "Username is valid":
        title.woeking += 1
        print("[" + Fore.GREEN + "INFO" + Fore.WHITE + f"] Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))
        with open("Names.txt", "a") as f:
            f.write(f"{name}\n")
            f.close()
    else:
        title.notworking += 1
        print("[" + Fore.RED + "INFO" + Fore.WHITE + f"] Not Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))


def GeorgeFloyd():
    firstPart = "George"
    # the best is 2 up
    random_shit = ("").join(random.choice(string.ascii_letters) for k in range(2))
    EndPart = "Floyd"
    name = f"{firstPart}{random_shit}{EndPart}"
    checker = requests.get(
        f"https://auth.roblox.com/v2/usernames/validate?request.username={name}&request.birthday=2000-01-01&request.context=Signup",
        proxies={"http": 'http://' + getProxy()}).json()
    code1 = checker['message']

    if code1 == "Username is valid":
        title.woeking += 1
        print("[" + Fore.GREEN + "INFO" + Fore.WHITE + f"] Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))
        with open("Names.txt", "a") as f:
            f.write(f"{name}\n")
            f.close()
    else:
        title.notworking += 1
        print("[" + Fore.RED + "INFO" + Fore.WHITE + f"] Not Working {name}")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))


def Ishootniggasforfun():
    firstPart = "nig"
    # the best is 2 up
    random_shit = ("").join(random.choice(string.ascii_letters) for k in range(2))
    EndPart = "gas"
    name = f"{firstPart}{random_shit}{EndPart}"
    checker = requests.get(
        f"https://auth.roblox.com/v2/usernames/validate?request.username=Ishoot{name}forfun&request.birthday=2000-01-01&request.context=Signup",
        proxies={"http": 'http://' + getProxy()}).json()
    code1 = checker['message']

    if code1 == "Username is valid":
        title.woeking += 1
        print("[" + Fore.GREEN + "INFO" + Fore.WHITE + f"] Working Ishoot{name}forfun")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))
        with open("Names.txt", "a") as f:
            f.write(f"Ishoot{name}forfun\n")
            f.close()
    else:
        title.notworking += 1
        print("[" + Fore.RED + "INFO" + Fore.WHITE + f"] Not Working Ishoot{name}forfun")
        system("title Working: " + str(title.woeking) + " Not Working: " + str(title.notworking))


def main():
    system("cls")
    logo_names = ("""
    [1] Nigger
    [2] Nigga
    [3] Fuck
    [4] Bitch
    [5] Shit
    [6] Ass
    [7] George Floyd
    [8] I shoot niggas for fun
    [9] Ass Beater
    [10] pussy
    [11] pussyeater
    [12] All

    """)
    print(logo_names)
    class_map = {
        "1": nigger,
        "2": Nigga,
        "3": Fuck,
        "4": Bitch,
        "5": Shit,
        "6": Ass,
        "7": GeorgeFloyd,
        "8": Ishootniggasforfun,
        "9": assbeater420,
        "11": pussylicker,
        "12": "all"
    }


    name_choice = input("What Name Do You Want? ")
    selected_class = class_map.get(name_choice)

    if selected_class:
        if selected_class == "all":
            classes = [nigger, Nigga, Fuck, Bitch, Shit, Ass, GeorgeFloyd, Ishootniggasforfun, assbeater420, pussylicker]
            while True:
                if threading.active_count() <= amount:
                    for class_func in classes:
                        try:
                            threading.Thread(target=class_func, daemon=True).start()
                        except:
                            pass
        else:
            while True:
                loop_choice = input("Do you want to loop? (y/n) ")
                if loop_choice.lower() == "y":

                    while True:
                        if threading.active_count() <= amount:
                            try:
                                threading.Thread(target=selected_class, daemon=True).start()
                            except:
                                pass
                elif loop_choice.lower() == "n":
                    selected_class()
                    break
                else:
                    print("Invalid choice. Please enter 'y' or 'n'.")
    else:
        print("Invalid choice")

main()

#made the code look better so no more if 1 then so on and made a cool thing that ask if you wanna loop it or no :)
